import { requireAdmin } from "@/lib/auth/admin";

export const metadata = { title: "CMS Settings" };
export default async function SettingsPage() {
  await requireAdmin();
  const configured = Boolean(process.env.NEXT_PUBLIC_SUPABASE_URL && process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY);
  return <div className="page-wrap cms-page"><header className="cms-page-header"><div><p className="eyebrow">Studio configuration</p><h1 className="page-title">Settings</h1><p className="subtle">A clear view of the systems behind publishing. Sensitive values are never shown.</p></div></header><div className="cms-service-grid"><article className="card panel"><p className="cms-kind">Database & authentication</p><h2 className="section-title">Supabase</h2><p className="subtle">Content, media metadata, revisions, and administrator access.</p><span className={`cms-status ${configured?"cms-status--published":""}`}>{configured?"Connected":"Setup required"}</span></article><article className="card panel"><p className="cms-kind">Publishing</p><h2 className="section-title">Pacific time</h2><p className="subtle">Scheduled dates are entered in America/Los_Angeles and stored as UTC timestamps.</p><span className="cms-status cms-status--published">Configured</span></article><article className="card panel"><p className="cms-kind">Media</p><h2 className="section-title">Responsive variants</h2><p className="subtle">320px, 960px, 1920px, and original asset references.</p><span className="cms-status cms-status--published">Enabled</span></article><article className="card panel"><p className="cms-kind">Search engines</p><h2 className="section-title">SEO defaults</h2><p className="subtle">Canonical domain: tyroneperez.com. Every entry can override title, description, social image, canonical URL, and robots behavior.</p><span className="cms-status cms-status--published">Configured</span></article></div></div>;
}
